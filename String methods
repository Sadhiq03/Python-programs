str = input("enter a string")
print(len(str))
print(str.upper())
print(str.lower())
print(str.swapcase())
string=(len(str))
alpha=0
digit=0
for i in str:
      if(i.isdigit()):
            alpha=alpha+1
      digit=alp-alpha
print("The number of digits is:")
print(alpha)
print("The number of alphabets is:")
print(digit)
